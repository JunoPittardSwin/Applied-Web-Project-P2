# Shared configuration for the Docker PHP server used in both deployment profiles.

[php]

# The company is situated in Melbourne, so we should show dates relative to there.
date.timezone = 'Australia/Melbourne'
